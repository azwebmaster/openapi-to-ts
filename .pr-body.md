## Summary
Clean up stale snapshot entries in generator tests.

## What I found
- No open GitHub issues currently.
- Test suite was passing, but Vitest reported obsolete snapshots in `src/generator.test.ts`.

## Changes
- Removed 3 obsolete snapshots from:
  - `src/__snapshots__/generator.test.ts.snap`

## Validation
- `bun run test` ✅ (191 tests passing)
- `bunx vitest run --update` removed obsolete snapshot entries and now reports clean snapshots.
